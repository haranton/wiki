---
order: 4
title: Пакетный запрос
---

```
Функция ТекстыЗапросовРазоваяВыгрузка()
	  
	  ТекстЗапроса = Новый СписокЗначений;
	  ТекстЗапроса.Добавить(	
	  "ВЫБРАТЬ РАЗЛИЧНЫЕ
	  |	втНоменклатура.Ссылка.ТоварнаяКатегория.НоменклатурнаяГруппа КАК Ссылка
	  |ИЗ
	  |	втНоменклатура КАК втНоменклатура", "НоменклатурныеГруппы");
	  Возврат ТекстЗапроса;
	  
КонецФункции
```

Затем в коде

```
	  Запрос.УстановитьПараметр("Дата1", Дата(2024,01,01)); 
	  Запрос.УстановитьПараметр("Дата2", ТекущаяДата());
	  Выборки = ИнтеграцияИС.ВыполнитьПакетЗапросов(Запрос, ТекстыЗапросов); 
```


