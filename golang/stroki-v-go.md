---
order: 0.7
title: Строки в go
---

Строки это набор байт то есть \[\]byte

`byte` -- это алиас для `uint8`, т.е. хранит **один байт**.

`rune` -- это **синоним типа** `int32`, который хранит **кодовую точку Unicode**.



Строка (`string`) в Go -- это **набор байт**.